// this component if the current user has already existing items listed
 
export default function Sell_CurrentPage() {

    return (
        <div style = {{fontWeight: "bold"}}> 
            View Orders
        </div>
    )
}